<head><script src="https://kit.fontawesome.com/acdf84f6df.js" crossorigin="anonymous"></script></head>

<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/"><span>Bookers</span></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
          <li><a class="fas fa-home nav-link text-light" href="/"> Home</a></li>
          <li><a class="fas fa-link nav-link text-light" href="/home/about"> About</a></li>
          <li><a class="fas fa-book-open nav-link text-light"href="/users/sign_up"> Books</a></li>
          <li><a class="fas fa-sign-out-alt nav-link text-light" rel="nofollow" data-method="delete" href="/users/sign_out"> logout</a></a></li>
        </ul>
      </div>
    </div>
  </nav>
</header>

<main>

        <div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
<%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %>

<table class="table">
  <tbody><tr></tr>
  <tr>
	  <th>name</th>
	 <th><%= current_user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th></th>
  </tr>
</tbody></table>

<div class="row">
  <%= link_to 'aabababab', user_path(current_user.id) %>

  <a class="btn btn-outline-secondary btn-block fas fa-user-cog edit_user_44516" href="users/:id(.:format)"></a>

</div>
      <h2 class="mt-3">New book</h2>


  <div class="form-group">
<%= form_with model: @post_image, local: true do |f| %>


 <%= f.label :title %><br>
<%= f.text_area :shop_name %>
<br>
 <%= f.label :Body %><br>
<%= f.text_area :caption %>
<br>

<%= f.submit 'Create Book' %>
<% end %>
  </div>

</form>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
      <table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
<tr>
 <% @post_images.each do |post_image| %>
<tr>
<th><%= post_image.shop_name2%></th>
<th><%= post_image.caption %></th>
    <% end %>
    </tr>

  </thead>
  <tbody>
  </tbody>
</table>
    </div>
  </div>
</div>
      </main>