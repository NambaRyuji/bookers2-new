<%= render 'books/list', books: @books %>
<%= flash[:notice] %>
<main>

        <div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
 <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>

<table class="table">
  <tbody><tr></tr>
  <tr>
	  <th>name</th>
	 <th><%= @user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= @user.introduction %></th>
  </tr>
</tbody></table>

<div class="row">

  <%= link_to '', edit_user_path(@user.id), class:"btn btn-outline-secondary btn-block fas fa-user-cog edit_user_44516" %>



</div>
      <h2 class="mt-3">New book</h2>


  <div class="form-group">
<%= form_with model: @book, local: true do |f| %>


 <%= f.label :title %><br>
<%= f.text_area :title %>
<br>
 <%= f.label :Body %><br>
<%= f.text_area :body %>
<br>

<%= f.submit 'Create Book' %>
<% end %>
  </div>

</form>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
      <table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th></th>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
<tr>
 <% @books.each do |book| %>
<tr>

<th><%= attachment_image_tag book.user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %></th>
<th><%= link_to book.title, book_path(book.id) %></th>
<th><%= book.body %></th>
<th><%= link_to "Edit", edit_book_path %></th>
<th><%= link_to "Destroy",destroy2_book_path , method: :delete,
"data-confirm" => "Are you sure?" %></th>
<% end %>

    </tr>

  </thead>
  <tbody>
  </tbody>
</table>
    </div>
  </div>
</div>
      </main>


            <footer>
      <div class="container mt-5">
  	<div class="row">
    	<div class="mx-auto">
    		<p>CopyRight Infratop.inc</p>
    	</div>
    </div>
  </div>
</footer>